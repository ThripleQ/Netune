--- /data/user/work/netune_extracted/Netune-beta/src/infra/config.c	2026-07-28 16:23:54.000000000 +0000
+++ /workspace/Netune-beta/src/infra/config.c	2026-07-29 09:06:39.775885373 +0000
@@ -65,6 +65,7 @@
     yyjson_val *v = cfg->root;
 
     char *k = strdup(key);
+    if (!k) return NULL;
     char *tok = strtok(k, ".");
     while (tok && v) {
         /* check for array index: key[idx] */
