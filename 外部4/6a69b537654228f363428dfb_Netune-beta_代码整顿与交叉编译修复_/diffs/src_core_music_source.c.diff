--- /data/user/work/netune_extracted/Netune-beta/src/core/music_source.c	2026-07-28 16:23:54.000000000 +0000
+++ /workspace/Netune-beta/src/core/music_source.c	2026-07-29 08:53:18.999057280 +0000
@@ -24,6 +24,8 @@
 
 void song_info_copy(SongInfo *dst, const SongInfo *src) {
     if (!dst || !src) return;
+    /* Free old strings to avoid leak when dst already holds data */
+    song_info_free(dst);
     *dst = *src;
     dst->id        = sdup(src->id);
     dst->source    = sdup(src->source);
